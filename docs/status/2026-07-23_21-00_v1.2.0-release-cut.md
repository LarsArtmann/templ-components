# Status Report: v1.2.0 Release Cut

**Date:** 2026-07-23 21:00
**Session goal:** Cut a new release from the 19 unreleased commits since v1.1.0
**Outcome:** v1.2.0 committed (`1abb2f7`) and tagged (`v1.2.0`, SSH-signed, verified) — **NOT pushed**

---

## What This Session Did (chronological)

1. Gathered release state: current version `1.1.0`, 19 commits unreleased, CHANGELOG `[Unreleased]` warm
2. Found 5 staged files blocking the release script (depguard removal, go-error-family v0.8 bump, breadcrumbs regen, docs reformat)
3. Discovered BuildFlow had auto-committed those staged changes as `d417814` while I was investigating — tree became clean
4. Ran build + test: **passed** (all 16 packages)
5. Ran lint: **FAILED — 71 issues** (godoclint: 49, ireturn: 12, testableexamples: 10)
6. Root-caused: commit `d417814` (BuildFlow auto-commit) intended to add `depguard` to the `.golangci.yml` `disable:` list but **replaced the entire list**, dropping `godoclint`, `ireturn`, `testableexamples` — three linters documented as "fundamentally incompatible, do NOT re-enable" in AGENTS.md
7. Fixed: restored all four entries to the `disable:` list
8. Verified lint: **0 issues**
9. Added missing CHANGELOG `[Unreleased]` entries: `navigation.SidebarNav`, go-error-family v0.8.0 bump, depguard removal
10. Hit BuildFlow pre-commit failure (parent `go.work` at `/home/lars/projects/go.work` interferes with `./...` pattern) — resolved with `GOWORK=off`
11. Committed CHANGELOG prep (`3effa35`)
12. Ran `scripts/release.sh 1.2.0` — script bumped version, moved CHANGELOG, ran full verify (build + test + lint all green), committed, tagged
13. Reviewed release commit + tag signature: all version files synchronized, tag SSH-signed and verified

---

## a) FULLY DONE

| #   | Task                                 | Evidence                                                                                                                                                                                                                          |
| --- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **v1.2.0 release cut**               | Commit `1abb2f7`, tag `v1.2.0` (annotated, SSH-signed, `git tag -v` = "Good signature"). `utils.Version = "1.2.0"`, CHANGELOG heading `## [1.2.0] — 2026-07-23`, FEATURES.md `**Version:** 1.2.0`. All three version files agree. |
| 2   | **Lint regression caught and fixed** | Commit `d417814` dropped 3 critical disabled linters from `.golangci.yml`. Restored in commit `52fabce` (BuildFlow auto-commit). `golangci-lint run` = 0 issues.                                                                  |
| 3   | **CHANGELOG entries completed**      | Added `navigation.SidebarNav` (the new component from commit `04267ee` that was missing from `[Unreleased]`), go-error-family v0.8.0 bump, depguard linter removal. Committed as `3effa35`.                                       |
| 4   | **Full verify suite green**          | Build (all 16 packages), test (all pass with `-race`), lint (0 issues), drift-guard tests (`TestVersionMatchesChangelog`, `TestVersionMatchesFeatures`, `TestDocsCountDrift`, `TestSkillComponentCount` = 98/98).                 |
| 5   | **`[Unreleased]` section reset**     | Release script moved body under `## [1.2.0]` heading and inserted a fresh empty `## [Unreleased]`.                                                                                                                                |

---

## b) PARTIALLY DONE

| #   | Task                             | What's done                                                  | What's missing                                                                                                                                                                                                                                                     |
| --- | -------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Push to remote**               | v1.2.0 commit + tag exist locally, tree clean, ready to push | **NOT pushed** — house rule says NEVER PUSH without explicit instruction. Local `master` is 22 commits ahead of `origin/master`.                                                                                                                                   |
| 2   | **CHANGELOG completeness audit** | Added SidebarNav + depguard + go-error-family entries        | Did NOT audit all 19 commits since v1.1.0 for missing entries. The CHANGELOG may still be incomplete — the 19 commits include sidebar nav refactoring, lint config reindenting, popover positioner, docs reconciliations that may or may not be fully represented. |

---

## c) NOT STARTED

| #   | Task                               | Why                                                                                                                                                                                                                  |
| --- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Demo CSS rebuild**               | The release script runs `templ generate` but does NOT rebuild the Tailwind CSS (`examples/demo/static/app.css`). If any component class strings changed in the 19 commits, the demo's CSS may be stale. Not checked. |
| 2   | **Git history cleanup**            | Two BuildFlow auto-commits (`d417814`, `52fabce`) between v1.1.0 and v1.2.0 have misleading/generic commit messages (see section d). Not addressed — would require interactive rebase which is destructive.          |
| 3   | **Post-release CHANGELOG warming** | The `[Unreleased]` section is now empty. Any work done after this point needs entries added immediately per the "warm at all times" rule.                                                                            |

---

## d) TOTALLY FUCKED UP

| #   | What                                                      | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Root Cause                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **BuildFlow commit `d417814` broke the lint gate**        | 71 lint failures (godoclint: 49, ireturn: 12, testableexamples: 10). If I had run `scripts/release.sh` without checking lint first, the script would have failed at step 7 (verify) and rolled back via the EXIT trap — wasting a full generate+build+test cycle (~30s). The broken config was in HEAD for the entire session until I caught it.                                                                                                                                                                                                  | BuildFlow auto-commits changes without understanding the semantic impact. The commit removed `depguard` from the `enable:` list AND intended to add it to `disable:`, but the YAML edit **replaced** the disable list instead of **appending** to it. The commit message even documents this as intentional: _"Remove godoclint, ireturn, testableexamples from the disabled list (these were previously explicitly disabled; with depguard gone the disable list is simplified to just depguard itself)"_ — this reasoning is **wrong**. Those three linters are disabled because they are fundamentally incompatible with a templ library, not because of depguard. |
| 2   | **BuildFlow commit `52fabce` has a hallucinated message** | The commit that restored the three disabled linters has the message: _"chore(ci): update golangci-lint configuration for improved code quality"_ with 7 bullet points of generic AI fluff ("Enhance linting rules", "Standardize code style enforcement", "Optimize linter settings for better performance on CI/CD pipelines"). None of the 7 bullets describes what actually happened (restoring 3 critical disabled linters that `d417814` broke). Future readers scanning git log will have no idea this commit fixed a lint gate regression. | BuildFlow auto-generates commit messages from diffs without understanding WHY the change was made. This is the same systemic problem documented in the prior session's status report (`docs/status/2026-07-22_18-51_pareto-execution-status.md` section d #1).                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | **Parent `go.work` interferes with BuildFlow pre-commit** | First commit attempt (`3effa35`) failed because BuildFlow's `go-generate` and `govalid-generate` steps run `go generate ./...` which resolves against `/home/lars/projects/go.work` (a parent workspace listing sibling projects but NOT `templ-components`). This produces: _"pattern ./...: directory prefix . does not contain modules listed in go.work"_. The release script works around this with `export GOWORK=off`, but the pre-commit hook does not.                                                                                   | `/home/lars/projects/go.work` exists at the parent directory level and Go's workspace mode auto-discovers it. The pre-commit hook doesn't set `GOWORK=off`. Every commit in this repo when run from the `templ-components` directory hits this unless the caller exports `GOWORK=off` first.                                                                                                                                                                                                                                                                                                                                                                          |

---

## e) WHAT WE SHOULD IMPROVE

| #   | Issue                                                        | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **BuildFlow commits broken code with confident messages**    | `d417814` broke the lint config and its commit message describes the breakage as a deliberate improvement. BuildFlow has no verification step between "edit file" and "commit". **Fix BuildFlow** (`larsartmann/buildflow`) to run `golangci-lint run` (or at minimum `golangci-lint config verify`) before auto-committing `.golangci.yml` changes. A config that produces 71 findings should not be committable.                                                         |
| 2   | **BuildFlow commit messages are hallucinated garbage**       | `52fabce` has 7 bullet points of AI-generated fluff that don't describe the actual change. **Fix BuildFlow** to either: (a) not auto-commit and let the user write the message, (b) use a simpler template like `"chore(buildflow): auto-format and lint <files>"` that doesn't pretend to explain the WHY. The current messages are worse than no message because they actively mislead.                                                                                  |
| 3   | **Parent `go.work` breaks pre-commit every time**            | Every `git commit` in this repo fails BuildFlow pre-commit unless `GOWORK=off` is exported. This is a papercut that wastes time on every commit. **Options:** (a) Add `export GOWORK=off` to the pre-commit hook or `.envrc`, (b) Add `templ-components` to `/home/lars/projects/go.work` `use()` list, (c) Set `GOWORK=off` in `flake.nix` devShell. Option (c) is cleanest — it's project-scoped and doesn't affect sibling projects.                                    |
| 4   | **I didn't audit all 19 commits for CHANGELOG completeness** | I added SidebarNav + depguard + go-error-family, but there are 16 other commits since v1.1.0. Some may contain user-facing changes not in the CHANGELOG. The `[Unreleased]` was already partially warm (popover fix, tooltip fix, HTMXSrc fix, etc.) but I treated it as complete without verifying against `git log v1.1.0..HEAD`. **For future releases:** run `git log v<prev>..HEAD --oneline` and cross-check every commit against the CHANGELOG body before cutting. |
| 5   | **The release script doesn't verify demo CSS freshness**     | `scripts/release.sh` runs `templ generate` + `go build` + `go test` + `golangci-lint` but does NOT rebuild the Tailwind CSS (`examples/demo/static/app.css`). If component class strings changed, the demo ships stale CSS. **Recommendation:** add a CSS build step to the release script, or document that the demo CSS is rebuilt only in the Docker pipeline (3-stage build overwrites it anyway).                                                                     |
| 6   | **CHANGELOG section headers had informal annotations**       | The `[Unreleased]` section had `### Fixed (post-v1.1.0 patch — not yet released)` and `### Added (post-v1.1.0)` instead of standard Keep a Changelog headers (`### Fixed`, `### Added`). I fixed these in commit `3effa35`, but they should never have been written that way — the version heading carries the release context, not the section header.                                                                                                                    |

---

## f) NEXT THINGS TO GET DONE (up to 50)

### Immediate (blocks the release from being useful)

1. **Push v1.2.0 to remote** — `git push origin master --follow-tags` (requires explicit user approval per house rule)
2. **Verify the Go module proxy serves v1.2.0** — after push, check `https://proxy.golang.org/github.com/larsartmann/templ-components/@v/list` shows `1.2.0`

### BuildFlow fixes (`larsartmann/buildflow`)

3. **Add lint verification to BuildFlow pre-commit** — run `golangci-lint config verify` (minimum) or `golangci-lint run` (full) before auto-committing config changes
4. **Fix BuildFlow commit message generation** — use honest templates, not hallucinated bullet-point essays
5. **Add `GOWORK=off` to BuildFlow's Go tool execution** — or detect workspace interference and handle it

### Repository quality

6. **Fix the parent `go.work` interference permanently** — set `GOWORK=off` in `flake.nix` devShell `shellHook`
7. **Audit all 19 commits since v1.1.0 against the CHANGELOG** — verify no user-facing changes were missed in v1.2.0
8. **Rebuild demo CSS** — run the Tailwind build to ensure `examples/demo/static/app.css` reflects current component classes
9. **Add demo CSS rebuild to `scripts/release.sh`** — or document that Docker handles it
10. **Clean up BuildFlow auto-commit messages** — `52fabce` and `d417814` have misleading messages in the v1.2.0 release range (would require interactive rebase — destructive, consider leaving as-is)

### Documentation

11. **Update AGENTS.md** — document the parent `go.work` interference as a known gotcha
12. **Update AGENTS.md** — note that BuildFlow's `templ-generate` step can silently break `.golangci.yml` (the disable-list replacement bug)
13. **Document the release checklist** — "audit `git log v<prev>..HEAD` against CHANGELOG before running `scripts/release.sh`"
14. **Update SKILL.md** — verify component count and SidebarNav is documented (test showed 98/98, but manual verification recommended)

### Testing

15. **Add a test that validates `.golangci.yml` disable list** — prevent the `d417814` regression: assert `godoclint`, `ireturn`, `testableexamples` are always in the disable list
16. **Add a test that validates lint passes clean** — CI already runs lint, but a dedicated `TestLintClean` drift-guard would surface regressions during development, not just CI
17. **Add CHANGELOG completeness test** — cross-reference `git log v<prev>..HEAD --oneline` feat/fix commits against CHANGELOG entries (hard to automate, but worth exploring)

### Dependency management

18. **Verify go-error-family v0.8.0 has no other consumer impact** — the breadcrumbs regeneration suggests API changes; check if other packages are affected
19. **Check for Dependabot vulnerability** — prior session noted "1 moderate vulnerability" never investigated
20. **Audit all dependencies for updates** — `go.mod` lists only 3 runtime deps; verify all are current

### Components

21. **Add SidebarNav to `tc ls` output** — verify the CLI tool lists the new component
22. **Add SidebarNav demo route** — `examples/demo` should showcase it
23. **Add SidebarNav to recipes** — could compose with `AppShell` for a dashboard recipe
24. **Add golden test for SidebarNav** — `navigation/sidebar_nav_test.go` exists but may not have golden file coverage

### Accessibility

25. **Audit SidebarNav for WCAG compliance** — verify aria-current on active item, keyboard navigation, landmark roles
26. **Verify SidebarNav dark mode compliance** — run `go test ./utils/... -run TestDarkMode`
27. **Verify SidebarNav motion-reduce compliance** — run `go test ./utils/... -run TestMotionReduce`

### CSS / Theming

28. **Verify SidebarNav uses logical CSS properties** — RTL/i18n compliance check
29. **Add SidebarNav to semantic token layer** — `templates/templ-components-theme.css`
30. **Add SidebarNav theme preset examples** — `templates/presets/`

### CI / Infrastructure

31. **Add `GOWORK=off` to CI workflow** — `.github/workflows/ci.yaml` may hit the same workspace interference
32. **Verify CI passes on v1.2.0** — after push, monitor CI run
33. **Add pre-commit hook for `.golangci.yml` integrity** — prevent disable-list regressions
34. **Consider adding `git config gpg.format ssh` to setup docs** — ensure contributors can sign tags

### Release tooling

35. **Add `--check` mode to `scripts/release.sh`** — dry-run that validates CHANGELOG warmth, version bump, and tree cleanliness without committing
36. **Add CHANGELOG diff to release script output** — show what's being released before committing
37. **Add release notes validation** — warn if entries are missing links to ADRs/docs

### Prior session carryover

38. **Golden file adoption (Pareto 4% tier)** — convert assertion-based snapshot tests to golden files
39. **gofmt → gofumpt switch** — `flake.nix` uses `gofmt`, `.golangci.yml` uses `gofumpt` (latent conflict)
40. **Pareto 20% tier items** — all remain unstarted
41. **Investigate the `.gitignore` BuildFlow managed section** — `# <<< buildflow-managed (.gitignore) <<<` exists; verify it's not hiding `*_templ.go` files

### Polish

42. **Add `navigation.SidebarNav` to README component list**
43. **Add SidebarNav to FEATURES.md component inventory** — verify count is correct
44. **Add SidebarNav usage example to docs** — similar to NavLink docs
45. **Consider adding `SidebarNavGroup` variant** — collapsible groups for deep nav trees
46. **Review if SidebarNav should integrate with `AppShell`** — currently standalone; could be the default sidebar content
47. **Add migration note for v1.1→v1.2** — if any breaking changes (none expected, but verify)
48. **Update ROADMAP.md** — mark v1.2 items as shipped
49. **Regenerate status report periodically** — this is the second status report; consider a cron/scheduled run
50. **Consider a `CHANGELOG.md` linter** — enforce Keep a Changelog format programmatically

---

## g) QUESTIONS (that I cannot figure out myself)

### 1. Should I push v1.2.0 to remote now?

The release is committed and tagged locally but not pushed. House rule says "NEVER PUSH TO REMOTE" without explicit instruction. Local `master` is 22 commits ahead of `origin/master`. The Go module proxy won't serve v1.2.0 until the tag is pushed.

**Do you want me to:** (a) push now (`git push origin master --follow-tags`), (b) wait for you to review first (`git show v1.2.0`), (c) you'll push yourself?

### 2. Is the parent `/home/lars/projects/go.work` intentional?

There's a workspace file at `/home/lars/projects/go.work` that lists 7 sibling projects (`cqrs-htmx`, `go-atomic-write`, `go-ndjson`, `go-sse`, `go-workflow-auditlog`, `samber-do-auditlog`) but NOT `templ-components`. This breaks `go generate ./...` and BuildFlow pre-commit in this repo unless `GOWORK=off` is set.

**Should I:** (a) add `templ-components` to the workspace `use()` list, (b) set `GOWORK=off` in `flake.nix` devShell, (c) leave it (you manage the workspace manually)?

### 3. Should the BuildFlow auto-commit messages be rewritten?

Two commits in the v1.2.0 release range (`d417814`, `52fabce`) have misleading or hallucinated commit messages from BuildFlow. `d417814` documents its lint-breaking change as intentional, and `52fabce` has 7 generic bullet points that don't describe the actual fix. These are permanently in the release history.

**Should I:** (a) leave them (rewriting requires interactive rebase = destructive), (b) fix BuildFlow's message generation so future commits are honest, (c) both?
