# Status Report — 2026-09-08 22:36 CEST — v1.15.1 Release Cut + Daemon Relapse

**Session scope:** One directive ("Time for a new version?") followed by "just get it done". Full
release lifecycle for v1.15.1, from readiness assessment through post-propagation go.sum sweep.
Report written while the auto-commit daemon was mid-cycle on its **fourth** regression flip of the day.

**Outcome in one line:** v1.15.1 is released, tagged (7 signed tags), propagated, and `go get`-able —
but master was re-poisoned by the daemon ~10 minutes after the release completed, and that structural
conflict is now the single most important open item.

---

## Timeline (all times CEST, 2026-09-08)

| Time    | Event                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~21:55  | Phase 0 assessment: 16 commits since v1.15.0, zero `feat:` → PATCH. Origin in sync (daemon had pushed the previously-unpushed ~11 commits — prior report's Q1 resolved itself).                                                                                                                                                                                                                                       |
| ~22:00  | **CI + Website RED on tip `755128a`**: CSS Freshness guard (app.css un-minified, 4986 lines) + Website frozen-lockfile failure (pins flipped to astro ^7.3.2 / html-validate ^11.15.0 / typescript ^7.0.2 without lockfile regen). Third daemon flip of the day (`7b8c6d1` → `b544bcd` → `8bcbabc`).                                                                                                                  |
| ~22:15  | Fixed both: pins restored to lockfile-matched `7.3.1 / 11.12.0 / 6.0.3`, CSS re-minified via `nix run .#css` (byte-stability verified), `pnpm install --frozen-lockfile` exit 0. Committed `eb1f0fe`.                                                                                                                                                                                                                 |
| ~22:25  | Asked user: push + cut? → "just get it done". eb1f0fe already on origin (daemon pushed it). CI + Website green on eb1f0fe.                                                                                                                                                                                                                                                                                            |
| ~22:27  | Pre-release verification: 6 sub-modules `GOWORK=off go test` pass, visualtest compiles, errorpage + root lint 0 issues. (charts/echarts false FAIL = my log-path bug, see §d.)                                                                                                                                                                                                                                        |
| ~22:30  | **Cut v1.15.1**: `nix shell nixpkgs#govulncheck -c nix develop -c scripts/release.sh 1.15.1 "..."` → release commit `d426648` (signed) + re-add-replaces commit `a46f935` + 7 annotated SSH-signed tags. Reviewed before push: version/changelog/features synced, zero `replace` in tagged go.mods.                                                                                                                   |
| ~22:32  | Pushed master + all tags (`--follow-tags`). Polled proxy → all 7 tags resolve.                                                                                                                                                                                                                                                                                                                                        |
| ~22:33  | `GOWORK=off go mod tidy` sweep → 5 go.sums refreshed → `c29b195` pushed. CI success on c29b195 (Website not triggered — go.sum-only commit).                                                                                                                                                                                                                                                                          |
| ~22:35+ | **Daemon relapse #4** (`c133896`, local-unpushed at report time): pins re-flipped, app.css un-minified again (+4986), `global.out.css` un-minified (+5447), plus a 73-line edit to the prior session's status report swept into the snapshot. `global.out.css` still dirty in tree — daemon mid-cycle. **No re-fix attempted: would race the daemon's in-flight snapshots, and §g-1 asks how you want this handled.** |

---

## a) FULLY DONE

| # | Work                                                                                                                                                                                                                                                                                                           | Evidence                                                                                                                |
| - | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 1 | **Master unblocked.** Daemon-regression fix `eb1f0fe`: website pins restored (astro ^7.3.1, html-validate ^11.12.0, typescript ^6.0.3 — lockfile-matched, TS 7 crashes `astro check`), demo CSS re-minified.                                                                                                   | CI success + Website success on eb1f0fe; `pnpm install --frozen-lockfile` exit 0; `nix run .#css` byte-stable on re-run |
| 2 | **v1.15.1 cut end-to-end.** One-commit release convention: `d426648` with release notes in body + CHANGELOG `[1.15.1] — 2026-09-08`; `utils.Version` = 1.15.1; FEATURES.md synced; script's full verify (generate + build + test + lint + drift guards) passed pre-commit; tagged go.mods have zero `replace`. | `git show v1.15.1` reviewed before push; script exited through commit+tag path                                          |
| 3 | **7 tags signed, pushed, immutable.** `v1.15.1` + `utils/` `icons/` `errorpage/` `charts/echarts/` `datastar/` `htmx/` `v1.15.1`.                                                                                                                                                                              | `git ls-remote --tags` = 14 refs (7 tags × annotated+peeled); no tag poisoning                                          |
| 4 | **Proxy propagation verified.** Root + all 6 sub-modules resolve at v1.15.1 on proxy.golang.org.                                                                                                                                                                                                               | `go list -m <module>@v1.15.1` × 7 → ALL-TAGS-PROPAGATED                                                                 |
| 5 | **Post-propagation go.sum sweep.** The five replace-less sub-modules' stale sibling checksums refreshed after tags landed (v1.11.0/v1.12.0 lesson applied, not repeated).                                                                                                                                      | `c29b195` pushed; CI success; the expected red on `a46f935` (pre-propagation) healed                                    |
| 6 | **Pre-release verification matrix.** 6 sub-module test suites (GOWORK=off), visualtest compile, `golangci-lint` 0 issues on errorpage + touched root packages, CI + Website green before cutting.                                                                                                              | Per-module loop output; lint exit 0                                                                                     |
| 7 | **Release hygiene per skill + checklist.** Pre-verified BEFORE release.sh (never let the script be the first gate); release commit reviewed before push; `--follow-tags` push; convention followed.                                                                                                            | This report's timeline                                                                                                  |

## b) PARTIALLY DONE

| # | Work                                 | State                                                                                                                                                                                       | Gap                                                                                                                                                                                  |
| - | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1 | **GitHub Releases page**             | Discovered mid-release that Releases lapsed after v1.12.0 (v1.13.x–v1.15.0 have none). Followed the recent tags-only convention.                                                            | Policy decision (backfill vs codify tags-only) = §g-2. No notes page for v1.15.1 exists.                                                                                             |
| 2 | **visualtest lint hygiene**          | Enumerated: wrapcheck 20, mnd 5, gosec 2, goconst 1, nolintlint 1 = 29 findings. Not fixed — visualtest sits outside the CI lint loop by design (AGENTS.md), so release gating was correct. | The nolintlint:1 may be a stale nolint from the prior session's erraudit work — quick win buried in there.                                                                           |
| 3 | **Structural daemon fix**            | Proposed BuildFlow path-exclude in my message before the release; user's "just get it done" scoped to the release.                                                                          | Zero research done on whether `larsartmann/buildflow` supports path ignores. Whack-a-mole continues meanwhile (4 flips today).                                                       |
| 4 | **Consumer-side release validation** | Proxy resolution verified for all 7 modules.                                                                                                                                                | pkg.go.dev page rendering not checked; scratch-module `go get`/`go build` smoke test not run; `scripts/check-release-tags.sh` guard not formally run (used `git ls-remote` instead). |
| 5 | **Post-fix state of master**         | Release artifacts clean and immutable; the 4th flip poisons only post-release master.                                                                                                       | One-command remediation staged (§f-3) but NOT executed — daemon mid-cycle at report time.                                                                                            |

## c) NOT STARTED

- **erraudit CLI vs BuildFlow divergence** (direct CLI finds 0; BuildFlow found 33 — version/config drift, uninvestigated; carried from prior session, untouched this session).
- **Harvest** of both 2026-09-08 reports' §f lists into `TODO_LIST.md`/`ROADMAP.md` (docs-health HARVEST).
- **Annotate** prior reports (docs-health ANNOTATE): e.g. the 22:07 report's open Q1 (push strategy) is now resolved — daemon pushed everything.
- **Dead code deletes** surfaced by gopls this session: `heroWireLine` (`examples/demo/main.go:156`), `regionExistsExpr` (`visualtest/wire_forms_pack_e2e_test.go:817`).
- **Website workflow paths-filter verification** — Website skipped c29b195 (go.sum-only); almost certainly a paths filter, unverified.
- **TS7 / astro check upstream tracking** — `typescript ^6.0.3` pin is a latent time bomb until `astro check` supports the native TS compiler.
- **CHANGELOG `[Unreleased]`** is empty post-release (correct); next real work must warm it again.

## d) TOTALLY FUCKED UP

| # | What                                                                                                                                                                                                                                                                                                                 | Why it matters                                                                                                                                                                           | Correction                                                                                                                                                                                                                       |
| - | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **My test harness lied in both directions simultaneously.** The per-module loop wrote logs to `/tmp/t-$mod.log`; for `charts/echarts` the slash broke the redirect → false `FAIL charts/echarts`, and the same broken path made the grep-based summary print `ALL-MODULE-TESTS-PASS` over a run that never executed. | A failed run reported as pass AND a pass reported as fail, in one output — while deciding release readiness.                                                                             | Caught within a minute: re-ran cleanly, `charts/echarts` exit 0. This is the **same documented gotcha from the prior session** ("bash log paths with module-name slashes break redirects") — repeated anyway. Root fix in §f-30. |
| 2 | **Asked permission for a moot action.** My go/no-go question led with "Push eb1f0fe?" — the daemon had already pushed it. I framed a decision around state I hadn't fetched.                                                                                                                                         | Wasted a user round-trip on a non-decision; erodes trust in the questions that DO matter (§g).                                                                                           | Check `git fetch` + `origin/master` BEFORE framing any push-related question.                                                                                                                                                    |
| 3 | _(External, but session-defining)_ **Daemon relapse #4 minutes after the fix.** c133896 re-flips pins + un-minifies both CSS files AND swept an unrelated 73-line edit to the prior session's status report into its snapshot. Working tree still churning as this report was written.                               | Proves the per-incident fix loop is futile: 4 flips/day, each blocking CI. Also means daemon snapshots are swallowing unrelated working-tree edits — contamination, not just regression. | Not fixed this session (mid-cycle race + §g-1). Remediation staged as §f-3.                                                                                                                                                      |

## e) WHAT WE SHOULD IMPROVE

1. ~~**Structural daemon defense is no longer optional.** Four identical incidents in one day; each fix is 2 minutes, each re-flip re-blocks CI. Path-exclude in BuildFlow (or a repo-level guard that fails fast) is the only exit.~~ done (docs-health pass 2026-09-08)
2. ~~**Bundle the release push.** The daemon raced `a46f935` to origin ahead of me → a red CI run is now permanently in master's history (the documented pre-propagation red). Pushing release commit + re-add commit + tags in ONE push (and accepting the go.sums follow-up) would keep history green.~~ done (docs-health pass 2026-09-08)
3. ~~**Script the post-daemon-commit re-check trio** (CSS byte-stability, website pins-vs-lockfile, `global.out.css`): they live as AGENTS.md prose, needed 4× today, and each incident relied on a human/agent remembering them.~~ done (docs-health pass 2026-09-08)
4. ~~**Never frame questions from stale state** — fetch origin first (see §d-2).~~ done (docs-health pass 2026-09-08)
5. ~~**One shared multi-module test script** with sanitized log names kills the slash-path bug class (§d-1) permanently.~~ done (docs-health pass 2026-09-08)
6. ~~**GitHub Releases policy** needs an explicit decision so releases stop being adjudicated ad hoc (§b-1).~~ done (docs-health pass 2026-09-08)
7. ~~**release.sh could own the post-propagation sweep** (poll `go list -m`, tidy, commit) instead of externalizing it as tribal knowledge — it already warns about it.~~ done (docs-health pass 2026-09-08)
8. ~~**Daemon snapshot contamination**: c133896 swept a 73-line foreign edit into a heuristic commit. Snapshots should exclude `docs/status/` (or the daemon should not commit files it can't attribute).~~ done (docs-health pass 2026-09-08)

## f) NEXT TASKS (impact-sorted; top ~10 are the real queue, rest is backlog)

| #  | Task                                                                                                                                     | Impact      | Effort |
| -- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------ |
| 1  | Research BuildFlow config for path-ignore support; implement in `larsartmann/buildflow` if missing                                       | 🔴 critical | M–L    |
| ~~2~~  | ~~`scripts/daemon-regression-check.sh` (pins-vs-lockfile + CSS byte-stability + global.out.css), wired into pre-commit + CI~~ done — visualtest lint zero N4 | ~~🔴 critical~~ | ~~S~~ |
| 3  | Re-restore pins + re-minify `app.css`/`global.out.css` (4th remediation — staged, awaiting §g-1)                                         | 🔴 critical | S      |
| ~~4~~  | ~~GitHub Releases decision (§g-2) + execution (backfill v1.13.0–v1.15.1 or codify tags-only in release-checklist)~~ **Won't implement — superseded v1.16.0 smoke.** | ~~🟡 high~~ | ~~S–M~~ |
| ~~5~~  | ~~Scratch-module consumer smoke test: `go get` + build all 7 modules at v1.15.1~~ done — refixed 09-09 04-20 | ~~🟡 high~~ | ~~S~~ |
| 6  | Investigate erraudit CLI vs BuildFlow divergence (0 vs 33 findings)                                                                      | 🟡 high     | M      |
| 7  | Fix visualtest's 29 lint findings (start with the stale nolint) or document relaxed gate                                                 | 🟡 high     | M      |
| 8  | Provenance check on the 73-line edit to `2026-09-08_22-07` report swept into c133896 (§g-3)                                              | 🟡 high     | S      |
| 9  | Delete dead code: `heroWireLine`, `regionExistsExpr` (gopls unusedfunc)                                                                  | 🟢 med      | S      |
| 10 | Harvest §f of both 2026-09-08 reports into TODO_LIST/ROADMAP (docs-health HARVEST)                                                       | 🟢 med      | S      |
| 11 | Annotate prior status reports (ANNOTATE mode) — 22:07 report Q1 resolved, this report supersedes parts                                   | 🟢 med      | S      |
| 12 | Bundle-push step for release.sh (or daemon pause) to avoid pre-propagation red runs                                                      | 🟢 med      | S      |
| 13 | Move post-propagation tidy INTO release.sh (poll + tidy + commit)                                                                        | 🟢 med      | M      |
| 14 | Verify Website workflow paths filter (why c29b195 was skipped) is intentional                                                            | 🟢 med      | S      |
| 15 | Run `scripts/check-release-tags.sh` formally (guard exists; I used ls-remote)                                                            | 🟢 med      | S      |
| 16 | Verify pkg.go.dev rendering for v1.15.1 (all 7 modules)                                                                                  | 🟢 med      | S      |
| 17 | Track astro-check TS7 support upstream; plan the typescript ^7 flip                                                                      | 🟢 med      | S      |
| 18 | Warm CHANGELOG `[Unreleased]` when next real work lands                                                                                  | 🟢 med      | S      |
| 19 | AGENTS.md: update daemon-regression bullet (4th instance + chosen structural fix)                                                        | 🟢 med      | S      |
| 20 | `scripts/test-all-modules.sh` with sanitized log paths (kills slash-path bug class)                                                      | 🟢 med      | S      |
| 21 | Evaluate visualtest joining CI lint with a relaxed config instead of full exclusion                                                      | 🟢 med      | M      |
| 22 | Address 12 gopls diagnostics (stdversion jsonv2 = expected pre-Go-1.27; writestring inefficiencies)                                      | ⚪ low      | S      |
| 23 | Local `nix run .#visual` confirmation post-errorpage refactor (CI covered it)                                                            | ⚪ low      | S      |
| 24 | CI guard: fail if code changed but `[Unreleased]` empty (convention is unenforced)                                                       | ⚪ low      | S      |
| 25 | Evaluate dependabot/renovate for website deps (stop daemon-vs-lockfile drift class)                                                      | ⚪ low      | M      |
| 26 | Document today's release lessons in `docs/release-checklist.md` (bundle-push, gh release pre-check)                                      | ⚪ low      | S      |
| 27 | Confirm daemon quiescence before next work session                                                                                       | ⚪ low      | S      |
| 28 | `go get` validation note for GOEXPERIMENT=jsonv2 consumers in README (unchanged, re-confirm)                                             | ⚪ low      | S      |
| 29 | Reconsider whether `a46f935`-style red runs could be avoided by pushing tags first, commit second (proxy needs tags only) — needs design | ⚪ low      | M      |
| 30 | Add `git fetch` + origin-state check to release-checklist as a mandatory pre-phase-0 step                                                | ⚪ low      | S      |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **The daemon war — what's the endgame?** Do you want me to research and implement a path-ignore feature in `larsartmann/buildflow` itself (your tool), or only build repo-level guards (§f-1/2)? And until a structural fix exists: should I keep re-restoring the pins/CSS on every flip, or leave master red to force the structural fix? (Flip #4 happened while I wrote this report; I deliberately did not race its in-flight snapshots.)
2. **GitHub Releases policy:** Backfill releases for v1.13.0 → v1.15.1 with proper notes, or codify tags-only as the official convention (and note it in the release checklist)?
3. **The 73-line edit** to `docs/status/2026-09-08_22-07_buildflow-failure-triage-green-sweep.md` that the daemon swept into c133896 — was that you (or another session) editing the report intentionally? I can't determine authorship; per house rules I have not reverted it.

---

_Point-in-time snapshot. State as of writing: local master `c29b195` + daemon's unpushed `c133896`; tree has `global.out.css` dirty (daemon mid-cycle). Release artifacts (tags `v1.15.1` × 7, proxy-resolved) are unaffected by the relapse._
