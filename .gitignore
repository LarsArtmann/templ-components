# Generated templ files — MUST be committed for Go module proxy compatibility
!*_templ.go

# Build artifacts
/server
coverage.out

# Test coverage
/coverage

# Go vendor directory
/vendor

# IDE
.vscode/
.idea/

# OS
.DS_Store
# Dependencies
node_modules/
# Copy/paste detection report
/report/jscpd-report.json
# Build artifacts
/templ-components
/demo
# Sensitive files
*.db
# Nix build output
/result

# >>> buildflow-managed (.gitignore) >>>
# Auto-managed by buildflow. Edit outside these markers; this block is regenerated on `buildflow --fix`.

## Secrets and environment
.env
.env.local
.env.*.local
*.pem
*.key

## Databases
*.db-shm
*.db-wal

## Operating systems
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

## Editors and ides
*.swp
*.swo
*~

## Logs and temporaries
*.log
*.tmp
*.temp

## Buildflow artifacts
audit-log.*
workflow-audit-log.*
.buildflow-workflow-baseline.json

## Reports and coverage
reports/

## Go
*.exe
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum

## Nix
result-*
# .envrc is intentionally tracked: it only sets GOEXPERIMENT=jsonv2 and
# GOWORK=off (no secrets), and direnv needs it present for every clone so all
# tools (go, gopls, BuildFlow) pick up the flags. TestEnvrcConsistency guards it.
.direnv/

## Javascript and typescript
dist/
build/
*.tsbuildinfo
.next/
.nuxt/
.docusaurus/
.eslintcache
.cache/
.parcel-cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# <<< buildflow-managed (.gitignore) <<<

examples/demo/demo
tc
